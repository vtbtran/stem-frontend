# Firmware Files

Đặt file `onyx_base.bin` vào thư mục này.

Cách tạo file .bin:
1. Mở `OnyxBaseFirmware.ino` trong Arduino IDE
2. Menu: Sketch → Export Compiled Binary
3. Copy file `.bin` vào đây đổi tên thành `onyx_base.bin`
